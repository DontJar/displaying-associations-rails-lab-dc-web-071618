<h1> These are all the artists you may select from:</h1><br>

<ul>
<% @artists.each do |a| %>
  <li><%=link_to a.name, artist_path(a)%></li><br>
  <h3> <%= a.name %> has <%= a.song_count %> songs in the database</h3><br>
<% end %>
</ul>
